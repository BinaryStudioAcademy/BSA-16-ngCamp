.projectwrapper
  section.row.center-xs
    .main.col-xs-8
      h1 Project Settings
      .changeProj
        button(ng-click="proj.getProjects()",ng-if="proj.flag") Change project
        select(ng-if="!proj.flag",ng-model="proj.currentProject",ng-change="proj.setProject()")
          option(ng-repeat="project in proj.userProjects track by $index" value='{{project._id}}') {{project.title}}
      .goto
        a(href='') Trash
        a(href='') Reports
      form(name='projectEdit').row.center-xs
        .wrap
          label
            p Name:
          input(type='text', name='projectName')
        .wrap
          label
            p Description
          textarea
        .wrap
          label
            p Participants:
            i.fa.fa-user(aria-hidden='true')
          .participants(ng-model="proj.projectParticipants")
            p(ng-repeat="participator in proj.projectParticipants track by $index") {{participator}}
            a(ng-click="proj.addParticipatorFlag=true;")
              i.fa.fa-plus(aria-hidden='true')
            select(ng-if="proj.addParticipatorFlag",ng-model="proj.participatorToAdd",ng-change="proj.addParticipator()")
              option(ng-repeat="participator in proj.users track by $index") {{participator.email}}
        .wrap
          label
            p  Deadline:
            i.fa.fa-exclamation-triangle(aria-hidden='true')
          select#after-date.col-xs-6(name='')
            option 12 September 2016
            option 13 September 2016
        .wrap
          button.col-xs-6(type='submit', form='projectEdit', value='Submit') Save

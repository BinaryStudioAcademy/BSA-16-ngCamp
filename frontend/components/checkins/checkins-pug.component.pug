.checkins-wrapper
    // HEADER
    .checkins-header
        ul
            li.header-list
                h1 Check-ins
            li.header-list
                button(ng-link='["CheckinsCreate"]') Set up check-in
    // ACCORDION
    .checkins-accordion
        input#week-list(type='radio', name='week', checked='')
        input#monday(type='radio', name='week')
        input#tuesday(type='radio', name='week')
        input#wednesday(type='radio', name='week')
        input#thursday(type='radio', name='week')
        input#friday(type='radio', name='week')
        input#saturday(type='radio', name='week')
        input#sunday(type='radio', name='week')
        ul.tabs
            li
                label(for='week-list') QQ
            li
                label(for='monday') Mo
            li
                label(for='tuesday') Tu
            li
                label(for='wednesday') We
            li
                label(for='thursday') Th
            li
                label(for='friday') Fr
            li
                label(for='saturday') Sa
            li
                label(for='sunday') Su
        section.tab.all
            label(for='week-list') Questions List
            div(ng-repeat="checkin in check.checkIns track by $index")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-click="check.turnOn(checkin)",ng-checked="checkin.isTurnedOn",ng-model="checkin.isTurnedOn")
                                .slider.round
        section.tab.mo
            label(for='monday') Monday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Monday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
        section.tab.tu
            label(for='tuesday') Tuesday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Tuesday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
        section.tab.we
            label(for='wednesday') Wednesday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Wednesday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
        section.tab.th
            label(for='thursday') Thursday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Thursday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
        section.tab.fr
            label(for='friday') Friday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Friday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
        section.tab.sa
            label(for='saturday') Saturday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Saturday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
        section.tab.su
            label(for='sunday') Sunday
            div(ng-repeat="checkin in check.checkIns | filter: check.filterFunction('Sunday') ")
                ul.content
                    li.qq 12:30
                    li.qq
                        a(ng-link='["CheckinsInfo"]') {{checkin.question}}
                    li.qq
                        div
                            label.switch
                                input(type='checkbox', ng-disabled="true", checked='checked')
                                .slider.round
